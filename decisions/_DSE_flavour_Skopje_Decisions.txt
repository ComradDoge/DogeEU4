country_decisions = {

	skj_marko_inheritance = {
		major = yes
		potential = {
			tag = SKJ
			NOT = { has_country_flag = skj_claimed_marko_legacy }
		}
		provinces_to_highlight = {
			province_id = 3001 # Skopje
		}
		allow = {
			stability = 1
			is_at_war = no
			adm_power = 50
		}
		effect = {
			custom_tooltip = tooltip_line_seperate_general
			add_adm_power = -50
			add_prestige_or_monarch_power = { amount = 25 }

			custom_tooltip = tooltip_line_seperate_modifiers
			add_country_modifier = {
				name = skj_marko_legend
				duration = -1
			}

			custom_tooltip = tooltip_line_seperate_events
			country_event = { id = dse_flavour_skopje.1 }

			set_country_flag = skj_claimed_marko_legacy
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 60
	}
}
