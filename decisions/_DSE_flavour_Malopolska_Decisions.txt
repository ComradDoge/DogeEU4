country_decisions = {

	mpk_reclaim_the_wawel = {
		major = yes
		potential = {
			tag = MPK
			NOT = { has_country_flag = mpk_reclaimed_wawel }
		}
		provinces_to_highlight = {
			province_id = 262 # Krakow
			NOT = { owned_by = ROOT }
		}
		allow = {
			owns = 262 # Krakow
			is_at_war = no
		}
		effect = {
			custom_tooltip = tooltip_line_seperate_general
			262 = {
				add_core = MPK
			}
			add_prestige_or_monarch_power = { amount = 25 }

			custom_tooltip = tooltip_line_seperate_modifiers
			add_country_modifier = {
				name = mpk_seat_of_kings
				duration = -1
			}

			custom_tooltip = tooltip_line_seperate_events
			country_event = { id = dse_flavour_malopolska.1 }

			set_country_flag = mpk_reclaimed_wawel
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 60
	}
}
