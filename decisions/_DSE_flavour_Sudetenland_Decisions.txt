country_decisions = {

	sud_hold_the_line = {
		major = yes
		potential = {
			tag = SUD
			NOT = { has_country_flag = sud_held_the_line }
		}
		provinces_to_highlight = {
			province_id = 267 # Plzen
		}
		allow = {
			stability = 1
			is_at_war = no
			mil_power = 50
		}
		effect = {
			custom_tooltip = tooltip_line_seperate_general
			add_mil_power = -50
			add_prestige_or_monarch_power = { amount = 25 }

			custom_tooltip = tooltip_line_seperate_modifiers
			add_country_modifier = {
				name = sud_unbroken_walls
				duration = -1
			}

			custom_tooltip = tooltip_line_seperate_events
			country_event = { id = dse_flavour_sudetenland.1 }

			set_country_flag = sud_held_the_line
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 60
	}
}
